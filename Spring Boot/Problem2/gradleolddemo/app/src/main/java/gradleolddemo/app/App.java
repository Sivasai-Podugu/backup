/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package gradleolddemo.app;

import gradleolddemo.list.LinkedList;

import static gradleolddemo.utilities.StringUtils.join;
import static gradleolddemo.utilities.StringUtils.split;
import static gradleolddemo.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
